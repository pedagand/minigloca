a := 1;
b := a;
c := a + b;
while c < 100 do
	if b < 10 then
		a := b * 2
	else
		b := a * 2
	endif;
	d := 3 * c + b;
	if c > 100 then
		c := d + 1
	else
		c := c + 1
	endif;
	b := b + 3
done;
if b < 16 then
    d := 6;
    a := d * 6
else
    d := c
endif;
e := a